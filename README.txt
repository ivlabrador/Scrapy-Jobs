Install the packages in the virtualenv
Scrapy and Openpyxl

Put the command line: scrapy crawl jobspider

and the result is a xlsx file whit the last 10 pages of python jobs from jobtoday.com/es
